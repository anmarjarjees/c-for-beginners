#include <stdio.h>
#include <stdlib.h> // Include standard library
// Function prototype

// Function that returns a value, so this function will not have void
//  Allocating memory
int main()
{ /* variable that holds more than one value */
	void *pToSpaceInAllocateMemory;
	pToSpaceAlloactedMemory = malloc(2000);

	printf("Memory block starts at address: %p\n", pToSpaceInAllocateMemory);

	return 0;
}
